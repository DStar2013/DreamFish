        <h1>offsetParent    <strong>Version:cQuery_110421</strong></h1>

<h2>说明</h2>

<pre class="prettyprint"><code><span class="pln">cDom offsetParent</span><span class="pun">()</span><span class="pln"> </span></code></pre>

<p>cDom的<code>offsetParent</code>的方法，返回cDom元素最近的一个定位父元素</p>

<h2>参数</h2>

<p>无</p>

<h2>返回值</h2>

<p>cDom元素最近的一个定位父元素，如果没有返回body</p>

<h2>实例</h2>

<p>Example</p>

<pre class="prettyprint"><code><span class="pln">$</span><span class="pun">(</span><span class="str">'#id'</span><span class="pun">).</span><span class="pln">offsetParent</span><span class="pun">();</span></code></pre>

<p>输出</p>

<pre class="prettyprint"><code><span class="pun">返回</span><span class="pln">id</span><span class="pun">最近的一个定位父元素，如果有的话</span></code></pre>    