(function(window, $) {

	//
	CUIControls.CityInit({
		city: '#txtCity',
		source: '../js/data/c_cityData.js'
	});
	//
	CUIControls.DateInit({
		start: '#start',
		end: '#end'
	});

})(window, cQuery);